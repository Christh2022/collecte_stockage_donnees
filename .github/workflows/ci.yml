name: CI Pipeline

on:
    push:
        branches: [main, develop]
    pull_request:
        branches: [main, develop]

env:
    PYTHON_VERSION: "3.11"
    DOCKER_IMAGE: ghcr.io/${{ github.repository }}

jobs:
    # ==================== LINT ====================
    lint:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-python@v5
              with:
                  python-version: ${{ env.PYTHON_VERSION }}
                  cache: pip
            - run: pip install flake8
            - run: flake8 src/ --max-line-length=120 --exclude=__pycache__

    # ==================== TEST ====================
    test:
        runs-on: ubuntu-latest
        needs: lint
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-python@v5
              with:
                  python-version: ${{ env.PYTHON_VERSION }}
                  cache: pip
            - run: pip install -r requirements.txt && pip install pytest
            - run: pytest tests/ -v --tb=short

    # ==================== BUILD DOCKER ====================
    build:
        runs-on: ubuntu-latest
        needs: test
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
        steps:
            - uses: actions/checkout@v4
            - name: Lowercase image name
              run: echo "IMAGE_NAME=$(echo $DOCKER_IMAGE | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV
            - uses: docker/setup-buildx-action@v3
            - uses: docker/login-action@v3
              with:
                  registry: ghcr.io
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}
            - uses: docker/build-push-action@v5
              with:
                  context: .
                  push: true
                  tags: |
                      ${{ env.IMAGE_NAME }}:${{ github.sha }}
                      ${{ env.IMAGE_NAME }}:latest
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

    # ==================== DEPLOY AWS (désactivé — en attente du compte AWS) ====================
    # deploy-aws:
    #     runs-on: ubuntu-latest
    #     needs: build
    #     if: github.ref == 'refs/heads/main'
    #     environment: production
    #     steps:
    #         - uses: actions/checkout@v4
    #         - uses: aws-actions/configure-aws-credentials@v4
    #           with:
    #               aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
    #               aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    #               aws-region: ${{ secrets.AWS_DEFAULT_REGION }}
    #         - uses: actions/setup-python@v5
    #           with:
    #               python-version: ${{ env.PYTHON_VERSION }}
    #               cache: pip
    #         - run: pip install -r requirements.txt
    #         - name: Configure S3 encryption
    #           run: python -m infra.aws.setup_s3_encryption
    #           env:
    #               S3_BUCKET_NAME: ${{ secrets.S3_BUCKET_NAME }}
